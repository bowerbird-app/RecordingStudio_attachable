<%= turbo_frame_tag attachment_chrome_frame_dom_id(recording, kind:) do %>
  <%= render "recording_studio_attachable/attachment_chromes/chrome",
             **attachment_chrome_locals(
               recording:,
               return_to:,
               identity: {
                 kind: kind,
                 shape: shape,
                 size: size,
                 add_label: add_label,
                 change_label: change_label
               }
             ) %>
<% end %>
