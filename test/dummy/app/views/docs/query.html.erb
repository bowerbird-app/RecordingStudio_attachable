<div class="mx-auto flex w-full max-w-4xl flex-col gap-6">
  <%= render FlatPack::PageTitle::Component.new(
    title: "Query",
    subtitle: "Find whichever recordable type you care about, then filter to parent recordings that have matching attachments.",
    variant: :h1
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Any recordable type with images",
    subtitle: "Pass the target recordable type into the helper, then project the matching recording ids back to that model class.",
    anchor_link: true
  ) %>

  <%= render FlatPack::CodeBlock::Component.new(
    title: "Recordables with image attachments",
    code: @query_example,
    language: :ruby
  ) %>
</div>